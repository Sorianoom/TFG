# frontend/ (pendiente)

Aquí irá la interfaz **React + Vite** (modo IA OFF en la primera fase).

Consumirá la API del backend (`/api/attacks`, `/api/summary`) y mostrará:
- fichas por familia de ataque (señales v3, métricas, estado, limitaciones),
- resumen del proyecto (metodología, generalización, limitaciones, trabajo futuro).

Estilo: simple y moderno, tema oscuro si resulta sencillo. Aún no creado ni con dependencias.
